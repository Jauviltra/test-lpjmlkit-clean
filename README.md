test-lpjmlkit-clean

Quick start
- See `R/prepare_subset.R` if you want to run the model only for a subset of (non-adjacent) cells
- See `R/run_spain_subset_and_sim.R` for other customizations and running the model
